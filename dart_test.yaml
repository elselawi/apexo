tags:
  live_backend:
    description: Requires a configured live PocketBase server and credentials.
  serial:
    description: Uses shared Hive files, application singletons, or the shared live PocketBase server.
