// Copy this file to test/secret.dart and fill in your PocketBase test server credentials.
// test/secret.dart is .gitignored — never commit real credentials.

/// The URL of your PocketBase test server, e.g. 'http://127.0.0.1:8090'.
const testPBServer = 'http://127.0.0.1:8090';

/// Admin email for the test PocketBase server.
const testPBEmail = 'admin@example.com';

/// Admin password for the test PocketBase server.
const testPBPassword = 'your-admin-password';
